import { FaSortUp } from "react-icons/fa";
import { IconBaseProps } from "react-icons";
import React from "react";

interface Props extends IconBaseProps {}

const Up = (props: Props) => <FaSortUp {...props} />;

export default Up;
