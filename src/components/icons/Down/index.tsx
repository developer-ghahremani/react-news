import { FaSortDown } from "react-icons/fa";
import { IconBaseProps } from "react-icons";
import React from "react";

interface Props extends IconBaseProps {}

const Down = (props: Props) => <FaSortDown {...props} />;

export default Down;
