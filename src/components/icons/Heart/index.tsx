import { BsSuitHeart } from "react-icons/bs";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const Heart = (props: Props) => <BsSuitHeart {...props} />;

export default Heart;
