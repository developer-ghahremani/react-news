import { FiShare } from "react-icons/fi";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const Share = (props: Props) => <FiShare {...props} />;

export default Share;
