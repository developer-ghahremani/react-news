import { BsSun } from "react-icons/bs";
import { IconBaseProps } from "react-icons";

interface Props extends IconBaseProps {}

const Moon = (props: Props) => <BsSun {...props} />;

export default Moon;
